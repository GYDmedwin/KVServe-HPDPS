# KVServe_v1 重构总结

**一句话**：丢弃自研推理引擎，改由 vLLM V1 负责推理，KVServe_v1 专注 PD 间 KV 传输与压缩；核心新增代码仅 ~1,130 行。

---

## 关键决策与特性

1. **替换自研推理引擎（最核心）**  
   原项目自建了一套完整的推理引擎（`engine/`，5,166 行），包含调度、Worker、多流等复杂逻辑，维护成本高。  
   v1 改为接入 vLLM V1，通过实现 `KVConnectorBase_V1` 接口（~350 行）即可完成 PD 间 KV 传输，推理完全托管给 vLLM。

2. **非侵入式接入，升级兼容性强**  
   不修改 vLLM 任何源码，仅通过官方扩展点接入。后续 vLLM 版本升级，我们的代码基本无需改动。

3. **完整保留三种压缩模式**  
   `default`（内置配置）/ `custom`（自定义参数）/ `controller`（在线自适应选 profile）三种模式全部保留，业务功能无损。

4. **传输层稳定性重写**  
   原方案在 CUDA graph capture 场景下存在死锁风险，v1 采用 **ZMQ 控制信令 + NCCL 数据传输**，按需触发接收，彻底解决该问题。

5. **修复混合精度序列化问题**  
   压缩数据中含多种 dtype 的张量（uint8 压缩字节 + float32 量化参数），原 `EasyDist` 打包方案会将所有张量误判为同一 dtype 导致数据损坏。v1 改用 `pickle` + 设备迁移，确保传输正确性。

---

## 代码行数对比（不含 `tests/` 与 `__init__.py`）

| 部分 | 行数 | 说明 |
|------|------|------|
| 原项目 `kvserve` | **11,189** | 含自研引擎、simulator、eval |
| ↳ 其中自建引擎 (`engine/`) | 5,166 | 已全部丢弃，由 vLLM 替代 |
| ↳ 其中 simulator + eval | 2,384 | 已全部丢弃 |
| v1 新写核心代码 | **1,127** | connector + transport + KVCompressionAdapter + KV工具 |
| v1 迁入并精简的压缩组件 | **3,088** | 原 3,639 行 → 删除死代码后精简 551 行 |
| **v1 合计** | **4,215** | 相比原项目减少 **62%** |

> 如仅看"新增胶水代码"：KVServe_v1 接入 vLLM 只需 **~1,130 行**，其余是复用原有压缩组件。
